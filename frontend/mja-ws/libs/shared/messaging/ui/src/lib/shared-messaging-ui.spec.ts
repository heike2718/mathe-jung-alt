import { sharedMessagingUi } from './shared-messaging-ui';

describe('sharedMessagingUi', () => {
  it('should work', () => {
    expect(sharedMessagingUi()).toEqual('shared-messaging-ui');
  });
});
