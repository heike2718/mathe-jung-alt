import { raetselgruppenData } from './raetselgruppen-data';

describe('raetselgruppenData', () => {
  it('should work', () => {
    expect(raetselgruppenData()).toEqual('raetselgruppen-data');
  });
});
