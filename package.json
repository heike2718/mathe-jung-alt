{
  "name": "rbk-monorepo",
  "private": true,
  "scripts": {
    "fe": "npx nx run frontend:serve",
    "be": "npx nx run raetselbaukasten-api:dev",
    "latex": "npx nx run latex-service-api:build"
  },
  "devDependencies": {
    "nx": "22.5.2"
  }
}
